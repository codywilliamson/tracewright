namespace Tracewright.Core.Adapters;

/// <summary>
/// shared by all three v0.1 adapters (claude, git, raw) — bump when any adapter's translation changes.
/// </summary>
public static class AdapterVersion
{
    public const string Current = "0.1.0";
}
